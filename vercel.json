{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter ./frontend/app build-deps && pnpm --filter ./frontend/app build",
  "outputDirectory": "frontend/app/out",
  "rootDirectory": "."
}
